import React from 'react';
import { Box } from '@chakra-ui/react';

const SetupPage: React.FC = () => {
    return (
        <Box>
            <h1>Setup Page</h1>
            {/* Add PlayerForm and QuestionForm components here later */}
        </Box>
    );
};

export default SetupPage;
